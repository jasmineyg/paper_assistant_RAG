# Paper Assistant RAG v1 测试总结

本文档基于当前运行结果 `graph_mil_core_qa_v1_question_k10_20260607_163708` 总结 Paper Assistant RAG v1 的技术设计、能力边界、主要缺陷和测试集指标。

## 1. 当前模型的技术细节

Paper Assistant RAG v1 是一个面向本地论文知识库的命令行 RAG 系统。它不是单一大模型，而是由 PDF 解析、文本切分、向量索引、混合检索、对话记忆、Prompt 约束和 LLM 生成组成的完整问答流程。

### 1.1 文档读取与元数据保留

系统从 `data/paper/` 读取本地 PDF 论文。默认使用 `PyPDFLoader` 解析 PDF；如果 PyPDF 解析失败，会回退到 PyMuPDF 读取页面文本。

每一页都会保留以下元数据：

| 字段 | 含义 |
| --- | --- |
| `source` | PDF 文件名 |
| `source_path` | PDF 本地路径 |
| `page` | 页码 |

这些元数据会一直跟随文本片段进入索引和最终回答，因此回答可以追溯到具体论文、页码和 chunk。

### 1.2 文本切分

PDF 页面文本会被切成适合 embedding 和检索的片段。当前默认参数为：

| 参数 | 当前设计 |
| --- | --- |
| `chunk_size` | 1000 字符 |
| `chunk_overlap` | 180 字符 |
| splitter | `RecursiveCharacterTextSplitter` |
| separators | 段落、换行、句号、空格、空字符 |

切分后，每个 chunk 会新增 `chunk_id`，用于评测和来源定位。

### 1.3 Embedding 与 FAISS 向量索引

系统使用 embedding 模型把论文 chunk 转换成向量，并用 FAISS 建立本地向量索引。索引保存到 `vectorstore/faiss_index`，后续问答可以直接加载，不必重复解析 PDF 和重新生成 embedding。

当前支持两类 embedding provider：

| Provider | 说明 |
| --- | --- |
| OpenAI-compatible API | 默认路径，可通过 `.env` 配置 `OPENAI_BASE_URL` 和 `OPENAI_EMBED_MODEL` |
| Ollama | 本地 embedding，可配置 `OLLAMA_EMBED_MODEL` |

系统还支持增量追加新论文：追加时会跳过已经索引过的 PDF，只对新文档切分、embedding、加入 FAISS。

### 1.4 混合检索

当前检索不是单纯向量相似度，而是“向量检索 + 关键词检索 + RRF 融合”。

流程如下：

1. 向量检索：使用 FAISS 找出与问题 embedding 最相近的 chunk。
2. 关键词检索：从问题中抽取英文缩写、方法名等 anchor terms，例如 `migraph`、`gnn-mil`。
3. 变体扩展：对连字符、下划线、大小写形式做简单归一化，例如 `miGraph` 可扩展到 `mi-graph`。
4. 意图加权：如果问题包含“总结”“流程”等意图，会提高 `overview`、`method`、`workflow`、`step` 等词的权重。
5. RRF 融合：用 Reciprocal Rank Fusion 合并向量排序和关键词排序。当前权重为向量 1.0、关键词 2.0，说明系统更强调方法名、缩写、标题等精确命中。
6. Top-k 选择：评测中 `k=10`，即最终给 LLM 的候选来源为前 10 个 chunk。

### 1.5 References 过滤

论文的 References 部分很容易造成误召回：模型可能把“某篇论文被引用”误当成“当前论文提出了该方法”。因此系统默认降低参考文献列表 chunk 的优先级。

当前通过轻量规则判断 References-like chunk：

| 特征 | 作用 |
| --- | --- |
| 开头包含 `references` | 直接视为参考文献片段 |
| 年份数量较多 | 判断是否像引用列表 |
| 包含 proceedings、journal、IEEE、ACM、pp.、et al. 等标记 | 辅助识别参考文献列表 |

如果用户确实想查引用关系，可以通过 `--include-references` 放开过滤。

### 1.6 多轮问答与问题改写

系统使用 SQLite 保存对话历史，并通过 LangChain 的 history-aware retriever 做问题改写。对于“它的方法流程是什么”“其优缺点是什么”这类追问，系统会尝试根据历史对话把问题改写成可独立检索的问题。

当前记忆策略：

| 项 | 当前设计 |
| --- | --- |
| 存储 | SQLite |
| 最大历史消息数 | 12 |
| 用途 | 理解追问、代词和上下文 |
| 限制 | 事实依据仍必须来自本轮检索到的论文片段 |

### 1.7 回答生成与引用约束

最终回答由 chat LLM 基于检索到的 chunk 生成。Prompt 明确要求：

- 只能基于给定论文片段回答，不能编造。
- 不要把 References 中的被引用论文误当作当前论文方法。
- 必须用中文回答。
- 关键结论后必须带 `[S1]`、`[S2]` 等来源引用。
- 如果片段不足，要明确说明证据不足。
- 若问题涉及方法流程，要按 5-8 个步骤说明输入、处理方式、输出或作用。
- 最后单独说明不确定性。

系统还会做两个后处理：

| 后处理 | 作用 |
| --- | --- |
| 删除 `<think>...</think>` | 避免本地推理模型输出思考过程 |
| 引用兜底 | 如果模型没有写 `[Sx]`，则在答案末尾追加主要来源 |

## 2. 当前模型的能力

### 2.1 能稳定生成结构化中文回答

本次评测中，15 个 turn 全部生成了回答：

- `answer_present_rate = 1.0000`
- `citation_present_rate = 1.0000`
- 平均回答长度约 2594 字

这说明当前系统在“拿到检索片段后组织中文长回答”方面比较稳定，能够按照 prompt 给出分点说明、步骤拆解、优缺点总结和不确定性说明。

### 2.2 适合回答单篇论文的明确方法问题

当用户问题中包含明确论文名、方法名，并且目标论文能被检索命中时，系统表现较好。例如：

| 题目类型 | 表现 |
| --- | --- |
| RGMIL 是哪篇方法 | 目标论文命中，精确证据命中 |
| DSMIL 融合哪两类相似度 | 目标论文命中，精确证据命中 |
| BGNN-MIL 连的是哪些对象 | 目标论文命中，精确证据命中 |
| RGMIL vs DSMIL | 能召回两个方法相关证据 |

这类问题通常有清晰 anchor term，例如 `RGMIL`、`DSMIL`、`BGNN-MIL`，当前关键词增强检索能发挥作用。

### 2.3 适合论文方法流程类问题

系统 prompt 对“方法流程”有专门约束，会要求按步骤描述输入、处理、输出。因此当检索证据足够时，模型能比较自然地回答：

- 方法整体目标是什么。
- 输入数据是什么。
- 如何构图、编码或聚合。
- 最终如何做 bag-level prediction。
- 方法相比前作解决了什么问题。

### 2.4 适合做有限范围的跨论文比较

当前模型对两篇或少数几篇论文之间的比较有一定能力，尤其当两个目标都被检索到时，可以比较：

- GNN 范式 vs kernel 范式。
- graph uncertainty 的不同处理方式。
- 方法贡献、局限和适用场景差异。

本次跨论文比较类问题中，3 个 scored turn 的目标论文均至少命中一个，说明系统有一定比较基础。但多目标全召回仍然不足，见后文缺陷。

### 2.5 能处理部分知识库外问题

测试集中包含一个知识库外问题：`GraphMIL-Transformer++ 2026`。该题没有 gold target，因此不计入检索命中率。系统仍生成了带引用的回答，并应当基于现有片段说明“未找到直接证据”。这说明当前 prompt 具备一定拒答和不确定性表达能力，但这部分仍需要人工检查回答是否足够克制。

## 3. 当前模型的问题与缺陷

### 3.1 核心短板是检索定位，而不是回答生成格式

从指标看，回答生成和引用格式非常稳定，但检索指标偏低：

- 目标论文命中率只有 0.5714。
- 精确证据命中率只有 0.3571。
- 平均证据召回只有 0.1155。
- 证据 MRR 只有 0.0870。

这说明很多答案虽然写得完整、也带引用，但引用未必来自 gold evidence。换句话说，当前模型最大问题是“拿错材料后认真回答”。

### 3.2 缩写和大小写实体链接仍不稳

最典型失败是 `miGraph` vs `MIGraph`。测试要求区分 Zhou 2009 中两个不同实现：

- `MIGraph`：显式把每个 bag 构造成 graph，并设计 graph kernel。
- `miGraph`：通过 affinity matrix 隐式建图，使用更高效 graph kernel，并考虑 clique 信息。

但本次检索没有命中 Zhou 2009 原文，而是召回了许多后续论文中提到 `miGraph` 的片段，导致回答把 `MIGraph` 当成笔误或同一方法。这是严重语义错误，说明当前 anchor term 机制还不足以区分大小写、原始提出论文和后续引用论文。

### 3.3 方法族筛选能力较弱

测试集中有两类题目要求筛选方法族：

- 哪些方法真的用了 GNN，不要混入 graph kernel。
- 哪些是 kernel 方法，不是 GNN。

这两题均未命中目标论文或精确证据。原因是检索容易被 `graph`、`MIL`、`kernel`、`GNN` 等高频词干扰，召回了很多标题或正文中包含相似词的论文，但没有形成稳定的方法族判别能力。

这说明当前系统还缺少结构化论文知识，例如：

| 需要的结构化信息 | 当前状态 |
| --- | --- |
| 方法属于 GNN / kernel / attention / transformer 哪一类 | 主要依赖文本检索，未显式建模 |
| 方法别名和缩写 | 只做简单字符串变体 |
| 原始提出论文 vs 后续引用论文 | 没有强约束 |
| 方法之间的继承关系 | 依赖 LLM 从片段中临时归纳 |

### 3.4 跨论文多目标召回不足

跨论文比较问题中，系统经常只能命中其中一篇论文。例如 RGMIL 和 BGNN-MIL 的比较题，Top-10 基本被 RGMIL 占据，BGNN-MIL 没有充分进入上下文。

这导致回答容易出现两个问题：

- 对命中的一方讲得很细。
- 对未命中的一方只能泛泛而谈，或者基于间接片段推断。

当前 Top-k 选择没有按目标实体做覆盖控制，也没有为比较题做 query decomposition，因此多目标问题容易被单一高分论文“霸榜”。

### 3.5 多轮指代后的检索效果较差

多轮测试中，第一问 `MIL-GNN 的流程是什么？` 没有定位到 Tu 2019；第二问 `总结其优缺点` 也没有命中目标；第三问 `有哪些论文是在这个思路上继续改进的？` 只命中部分相关论文。

这说明 history-aware question rewrite 虽然存在，但在缩写变体和指代消解上还不够可靠。尤其当第一轮检索已经偏了，后续问题会沿着错误上下文继续偏移。

### 3.6 精确页码和 chunk 级证据召回偏低

目标论文命中率高于精确证据命中率，说明系统有时能找到正确论文，但找不到测试集指定的关键页和 chunk。

这会影响回答质量：

- 可能回答了论文大意，但漏掉关键公式、定义或实验结论。
- 对需要细节的题目，比如计算复杂度、邻接矩阵构造、具体相似度定义，容易不完整。
- 对学术阅读任务来说，chunk 级证据不足会削弱可核查性。

### 3.7 PDF 文本和编码质量会影响效果

当前系统依赖 PDF 抽取文本。若 PDF 内部字符编码、公式排版、连字符、大小写或标题信息抽取不稳定，会直接影响：

- embedding 表示。
- 关键词匹配。
- 缩写识别。
- 来源片段可读性。

本次运行产物中部分 JSON 显示存在中文编码异常，但 CSV 和 review markdown 中中文是正常的。未来仍应注意评测、导出和展示链路的编码一致性。

## 4. 测试集指标数值及解释

### 4.1 测试运行信息

| 项 | 数值 |
| --- | --- |
| Run ID | `graph_mil_core_qa_v1_question_k10_20260607_163708` |
| Dataset | `graph_mil_core_qa_v1` |
| Dataset version | `0.2.0` |
| Query field | `question` |
| Top-k | 10 |
| Include references | False |
| Answers generated | True |
| Items | 13 |
| Turns | 15 |

说明：测试集中有 13 个 item，其中包含一个多轮 item，因此展开后共有 15 个 turn。知识库外问题没有 gold target，不参与部分检索指标均值计算。

### 4.2 总体指标

| 指标 | 数值 | 解释 |
| --- | ---: | --- |
| `target_paper_hit_rate` | 0.5714 | 有 gold target 的 turn 中，Top-10 是否至少命中一个目标论文。约 57.14% 的题目找到了至少一篇正确论文。 |
| `all_target_papers_hit_rate` | 0.3571 | 多目标题中是否命中全部目标论文。只有约 35.71% 的题目把所有目标论文都召回。 |
| `exact_evidence_hit_rate` | 0.3571 | Top-10 是否命中测试集指定的精确 evidence chunk。只有约 35.71% 的题目找到了关键证据片段。 |
| `avg_target_paper_recall` | 0.4464 | 平均目标论文召回率。若题目有多篇目标论文，按命中的目标论文比例计算。 |
| `avg_evidence_recall` | 0.1155 | 平均证据 chunk 召回率。数值很低，说明虽然能找到部分相关论文，但关键证据覆盖不足。 |
| `mrr_target_paper` | 0.5079 | 第一个目标论文排名的平均倒数。越接近 1 越好；0.5079 表示命中时不少目标论文排得较靠前，但失败题也较多。 |
| `mrr_evidence` | 0.0870 | 第一个精确 evidence chunk 排名的平均倒数。很低，说明关键证据即使命中也常常不靠前，且大量题目未命中。 |
| `answer_present_rate` | 1.0000 | 所有 turn 都生成了非空回答。 |
| `citation_present_rate` | 1.0000 | 所有回答都包含 `[Sx]` 形式引用。 |
| `avg_answer_length` | 2594.0667 | 平均回答长度约 2594 字，说明模型倾向于生成较完整的长回答。 |

### 4.3 按题型聚合表现

| 类别 | 题数 | 目标论文命中 | 精确证据命中 | 评价 |
| --- | ---: | ---: | ---: | --- |
| `abbreviation_case_sensitivity` | 1 | 0/1 | 0/1 | 大小写缩写区分失败，miGraph/MIGraph 被混淆。 |
| `abbreviation_entity_linking` | 4 | 3/4 | 3/4 | 明确缩写实体链接总体较好，但 GNN-MIL/Tu 2019 未定位成功。 |
| `method_family_retrieval` | 1 | 0/1 | 0/1 | 方法族筛选失败，不能稳定筛出 GNN-based MIL。 |
| `false_recall_guard` | 1 | 0/1 | 0/1 | 反向筛选 kernel 方法失败，容易被 GNN 相关论文干扰。 |
| `paper_summary` | 1 | 1/1 | 0/1 | 单篇论文总结能找到目标论文，但没有命中指定精确证据。 |
| `cross_paper_comparison` | 3 | 3/3 | 2/3 | 跨论文比较至少能命中目标之一，但多目标全召回不足。 |
| `context_memory_summary` | 1 | 0/1 | 0/1 | 多轮第一问未定位到 Tu 2019。 |
| `context_memory_pros_cons` | 1 | 0/1 | 0/1 | 多轮代词追问未保持正确目标。 |
| `context_memory_followup` | 1 | 1/1 | 0/1 | 多轮后续改进论文只召回部分目标，证据不精确。 |

### 4.4 失败案例摘要

目标论文未命中的题目包括：

| ID | 问题类型 | 主要问题 |
| --- | --- | --- |
| `gmil-v2-001` | miGraph vs MIGraph | 未召回 Zhou 2009 原文，把大小写差异误判为笔误。 |
| `gmil-v2-002` | GNN-MIL 定位 | 未召回 Tu 2019 原文，只召回后续论文中对 GNN-MIL 的引用。 |
| `gmil-v2-006` | GNN 方法族筛选 | 召回了多个含 graph/MIL 的论文，但没有按方法范式稳定筛选。 |
| `gmil-v2-007` | kernel 方法族筛选 | 被 GNN 论文干扰，没有找到 kernel-based 目标集合。 |
| `gmil-v2-013:t1` | 多轮第一问 | `MIL-GNN` 缩写变体未映射到 Tu 2019 GNN-MIL。 |
| `gmil-v2-013:t2` | 多轮追问 | “其优缺点”没有继承正确论文目标。 |

### 4.5 指标解读结论

当前 Paper Assistant RAG v1 已经具备完整 RAG 工程形态，回答生成、中文结构化表达和引用格式都比较稳定。但从评测指标看，它还不是一个足够可靠的论文知识助手，因为检索阶段的目标论文召回和精确证据召回偏低。

可以把当前版本定位为：

- 适合：对已明确命中的单篇论文做总结、方法流程说明、贡献与局限分析。
- 勉强适合：少量论文之间的比较，但需要人工核验来源是否覆盖所有目标。
- 不适合直接信任：缩写大小写强区分、方法族全量筛选、多轮指代复杂追问、需要精确页码或公式证据的问题。

下一步优化应优先放在检索层，而不是继续加长回答 prompt。尤其建议加入方法别名表、论文级元数据、原始提出论文优先级、多目标 query decomposition、按论文去重与覆盖控制，以及对评测集中的 gold evidence 做召回误差分析。
